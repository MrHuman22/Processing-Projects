class Square{
  Pos pos;
  boolean onPath;
  int routes; // how many squares lead to this square
  boolean noMovesLeft;
  ArrayList<Pos> moves;
  
  Square(int col, int row){
    pos = new Pos(col, row);
    moves = new ArrayList<Pos>();
    onPath = false;
    noMovesLeft = false;
  
  }
  
  void update(){
  
  }
  
  void show(){
      
    ]
  
  void move(
}
